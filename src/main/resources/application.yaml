spring:
    mvc:
        hiddenmethod:
            filter:
                enabled: true
    profiles:
        active: em # options: dev, prod, em, list
    messages:
        basename: templates/messages
server:
    port: 8081
