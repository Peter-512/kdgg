spring:
    mvc:
        hiddenmethod:
            filter:
                enabled: true
    profiles:
        active: prod # options: dev, prod, em, list
    messages:
        basename: templates/messages
server:
    port: 8081
